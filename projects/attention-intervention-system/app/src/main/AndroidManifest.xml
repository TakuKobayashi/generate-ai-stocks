<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ════════════════════════════════════════════
         Permissions
         ════════════════════════════════════════════ -->

    <!-- 通知 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 再起動後の復元 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 正確なアラーム (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Android 13+の自動付与版（バッテリー最適化外） -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Foreground Service (基本) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+: specialUse タイプ明示 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Overlay表示 (SYSTEM_ALERT_WINDOW) - Hard モード時 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ════════════════════════════════════════════
         Application
         ════════════════════════════════════════════ -->
    <application
        android:name=".AISApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AIS"
        android:supportsRtl="true">

        <!-- ── Activities ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Foreground Service (Hard モード常駐) ── -->
        <!--
            Android 14以降は foregroundServiceType の明示が必須。
            "specialUse" は汎用ユースケース向け。
            Play Store申請時に用途説明が求められる。
        -->
        <service
            android:name=".service.InterventionForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Android 14+: specialUse の用途宣言 -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Displays goal text when screen turns on to help user maintain focus" />
        </service>

        <!-- ── BroadcastReceivers ── -->

        <!-- 再起動後 AlarmManager 復元 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- AlarmManager から毎朝6時に叩かれる -->
        <receiver
            android:name=".receiver.NotificationRefreshReceiver"
            android:exported="false" />

        <!--
            ScreenOnReceiver は ACTION_SCREEN_ON を受信する。
            Android 8.0以降、ACTION_SCREEN_ON は動的登録のみ有効。
            Manifest登録では受信できないため、ここには記載しない。
            InterventionForegroundService 内で動的登録する。
        -->

    </application>
</manifest>
