{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:full": "pnpm --filter frontend build:export && wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply nomikai-db",
    "vapid:generate": "npx tsx scripts/generate-vapid.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no eslint configured'"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "hono": "^4.3.0",
    "jose": "^5.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240422.0",
    "drizzle-kit": "^0.21.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.52.0"
  }
}
